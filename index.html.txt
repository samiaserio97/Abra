<!DOCTYPE html>

<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>abra — protege lo que construiste</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;1,400;1,500&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>

/* ── RESET & BASE ── */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; font-size: 16px; }

:root {
–cream:   #FAF8F5;
–linen:   #F2EEE8;
–fog:     #EAE6DE;
–burg:    #163C38;
–mid:     #0E2824;
–rose:    #3AADA8;
–blush:   #A8D4D0;
–muted:   #7A8E8C;
–ink:     #142420;
}

body {
background: var(–cream);
color: var(–ink);
font-family: ‘DM Sans’, sans-serif;
font-weight: 300;
overflow-x: hidden;
cursor: none;
}

/* custom cursor */
.cursor {
position: fixed; width: 8px; height: 8px;
background: var(–rose); border-radius: 50%;
pointer-events: none; z-index: 9999;
transform: translate(-50%,-50%);
transition: width .25s, height .25s;
}
.cursor-ring {
position: fixed; width: 30px; height: 30px;
border: 1px solid rgba(196,96,122,.3); border-radius: 50%;
pointer-events: none; z-index: 9998;
transform: translate(-50%,-50%);
}

/* ── NAV ── */
nav {
position: fixed; top: 0; left: 0; right: 0; z-index: 100;
padding: 24px 60px;
display: flex; align-items: center; justify-content: space-between;
background: rgba(250,248,245,.94);
backdrop-filter: blur(16px);
border-bottom: 1px solid rgba(74,17,40,.07);
}
.nav-logo {
font-family: ‘Lora’, serif;
font-weight: 300;
font-size: 1.5rem;
letter-spacing: .42em;
color: var(–burg);
text-decoration: none;
}
.nav-links { display: flex; gap: 40px; align-items: center; }
.nav-link {
font-size: .72rem; font-weight: 400;
letter-spacing: .18em; text-transform: uppercase;
color: var(–muted); text-decoration: none;
transition: color .2s;
}
.nav-link:hover { color: var(–burg); }
.nav-cta {
font-size: .7rem; font-weight: 500;
letter-spacing: .14em; text-transform: uppercase;
color: var(–cream); background: var(–burg);
padding: 10px 24px; border-radius: 100px;
text-decoration: none;
transition: background .25s;
}
.nav-cta:hover { background: var(–mid); }

/* ── HERO ── */
.hero {
min-height: 100vh;
padding: 160px 60px 100px;
display: grid;
grid-template-columns: 1fr 1fr;
gap: 80px;
align-items: center;
position: relative;
}
.hero::after {
content: ‘’;
position: absolute;
top: 120px; right: 0;
width: 38vw; height: 70vh;
background: var(–linen);
border-radius: 200px 0 0 200px;
z-index: 0;
}
.hero-left { position: relative; z-index: 1; }
.hero-eyebrow {
font-size: .68rem; font-weight: 500;
letter-spacing: .32em; text-transform: uppercase;
color: var(–rose); margin-bottom: 32px;
display: flex; align-items: center; gap: 14px;
animation: fadeUp .8s ease both;
}
.hero-eyebrow::before {
content: ‘’; width: 28px; height: 1px; background: var(–rose);
}
.hero-wordmark {
font-family: ‘Lora’, serif;
font-weight: 300;
font-size: clamp(5rem, 9vw, 9rem);
letter-spacing: .42em;
color: var(–burg);
line-height: 1;
display: block;
animation: fadeUp .9s .06s ease both;
}
.hero-headline {
font-family: ‘DM Sans’, sans-serif;
font-style: normal;
font-weight: 300;
font-size: clamp(2rem, 3.5vw, 3rem);
color: var(–ink);
line-height: 1.25;
margin-top: 24px;
max-width: 480px;
animation: fadeUp .9s .12s ease both;
letter-spacing: -.01em;
}
.hero-headline em { color: #3AADA8; font-style: italic; }
.hero-body {
font-size: 1rem; line-height: 1.9;
color: var(–muted); margin-top: 28px;
max-width: 420px;
animation: fadeUp .9s .18s ease both;
}
.hero-actions {
display: flex; gap: 20px; align-items: center;
margin-top: 44px;
animation: fadeUp .9s .24s ease both;
}
.btn-primary {
font-size: .72rem; font-weight: 500;
letter-spacing: .14em; text-transform: uppercase;
color: var(–cream); background: var(–burg);
padding: 14px 32px; border-radius: 100px;
text-decoration: none; transition: background .25s;
}
.btn-primary:hover { background: var(–mid); }
.btn-ghost {
font-size: .72rem; font-weight: 400;
letter-spacing: .14em; text-transform: uppercase;
color: var(–burg); text-decoration: none;
padding-bottom: 3px;
border-bottom: 1px solid rgba(74,17,40,.3);
transition: border-color .2s;
}
.btn-ghost:hover { border-color: var(–burg); }

.hero-right {
position: relative; z-index: 1;
display: flex; flex-direction: column; gap: 16px;
animation: fadeIn 1s .3s ease both;
}
.hero-stat {
background: var(–cream);
border: 1px solid rgba(74,17,40,.08);
border-radius: 16px;
padding: 28px 32px;
display: flex; flex-direction: column; gap: 6px;
}
.hero-stat-num {
font-family: ‘Lora’, serif;
font-size: 2.8rem; font-weight: 300;
letter-spacing: .06em; color: var(–burg); line-height: 1;
}
.hero-stat-label {
font-size: .75rem; color: var(–muted); letter-spacing: .06em;
}
.hero-stat-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }

/* ── INTRO STRIP ── */
.intro-strip {
background: var(–burg);
padding: 48px 60px;
display: flex; align-items: center; justify-content: space-between;
gap: 40px;
overflow: hidden;
}
.intro-strip-text {
font-family: ‘Cormorant Garamond’, serif;
font-style: normal; font-weight: 300;
font-size: clamp(2.4rem, 4.2vw, 3.8rem);
color: var(–cream); line-height: 1.15;
max-width: 760px;
}
.intro-strip-text em { color: #3AADA8; font-style: italic; }
.intro-strip-logo {
font-family: ‘Lora’, serif;
font-weight: 300; font-size: 1.2rem;
letter-spacing: .42em; color: rgba(245,240,235,.35);
white-space: nowrap;
}

/* ── SECTION BASE ── */
.section { padding: 120px 60px; max-width: 1280px; margin: 0 auto; }
.section-eyebrow {
font-size: .62rem; font-weight: 500;
letter-spacing: .36em; text-transform: uppercase;
color: var(–rose); margin-bottom: 20px;
display: flex; align-items: center; gap: 12px;
}
.section-eyebrow::before { content: ‘’; width: 18px; height: 1px; background: var(–rose); }
.section-title {
font-family: ‘Lora’, serif;
font-weight: 300;
font-size: clamp(2.4rem, 4vw, 3.6rem);
color: var(–ink); line-height: 1.15;
max-width: 680px;
}
.section-title em { font-style: italic; color: #3AADA8; }

/* ── SERVICIOS ── */
.servicios-grid {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 2px;
margin-top: 64px;
border-radius: 20px;
overflow: hidden;
}
.servicio-card {
background: var(–linen);
padding: 52px 48px;
position: relative;
transition: background .28s;
cursor: default;
}
.servicio-card:hover { background: var(–fog); }
.servicio-card.featured {
background: var(–burg); grid-column: 1 / 3;
display: grid; grid-template-columns: 1fr 1fr;
gap: 60px; align-items: center;
}
.servicio-num {
font-family: ‘Lora’, serif;
font-style: italic; font-size: .9rem;
color: var(–rose); margin-bottom: 20px; display: block;
}
.servicio-card.featured .servicio-num { color: var(–blush); opacity: .7; }
.servicio-title {
font-family: ‘Lora’, serif;
font-weight: 300;
font-size: 1.9rem; color: var(–ink);
line-height: 1.2; margin-bottom: 16px;
}
.servicio-card.featured .servicio-title { color: var(–cream); font-size: 2.4rem; }
.servicio-body {
font-size: .88rem; line-height: 1.85;
color: var(–muted); max-width: 380px;
}
.servicio-card.featured .servicio-body { color: rgba(245,240,235,.6); }
.servicio-items {
display: flex; flex-direction: column; gap: 10px;
margin-top: 24px;
}
.servicio-item {
font-size: .8rem; color: var(–muted);
display: flex; align-items: center; gap: 10px;
transition: color .2s;
}
.servicio-card:hover .servicio-item { color: var(–ink); }
.servicio-card.featured .servicio-item { color: rgba(245,240,235,.55); }
.servicio-item::before {
content: ‘’; width: 14px; height: 1px;
background: var(–rose); flex-shrink: 0;
}
.servicio-card.featured .servicio-item::before { background: var(–blush); opacity: .6; }
.servicio-arrow {
position: absolute; bottom: 32px; right: 36px;
font-size: 1.2rem; color: rgba(74,17,40,.2);
transition: color .2s, transform .2s;
}
.servicio-card:hover .servicio-arrow { color: var(–rose); transform: translateX(4px); }

/* ── PARA QUIEN ── */
.para-quien { background: var(–linen); padding: 120px 0; }
.para-quien .section { padding-top: 0; padding-bottom: 0; }
.perfiles-grid {
display: grid;
grid-template-columns: repeat(4, 1fr);
gap: 24px;
margin-top: 64px;
}
.perfil-card {
padding: 36px 28px;
border: 1px solid rgba(74,17,40,.1);
border-radius: 16px;
background: var(–cream);
display: flex; flex-direction: column; gap: 14px;
transition: border-color .25s, transform .25s;
}
.perfil-card:hover { border-color: var(–rose); transform: translateY(-4px); }
.perfil-icon {
width: 44px; height: 44px;
border-radius: 50%;
background: var(–linen);
display: flex; align-items: center; justify-content: center;
font-size: 1.2rem;
}
.perfil-title {
font-family: ‘Lora’, serif;
font-weight: 300; font-size: 1.3rem;
color: var(–ink); line-height: 1.2;
}
.perfil-body {
font-size: .8rem; line-height: 1.8;
color: var(–muted); flex: 1;
}
.perfil-tag {
font-size: .65rem; font-weight: 500;
letter-spacing: .14em; text-transform: uppercase;
color: var(–rose);
}

/* ── COMO FUNCIONA ── */
.proceso-steps {
display: grid;
grid-template-columns: repeat(3, 1fr);
gap: 2px;
margin-top: 64px;
border-radius: 20px;
overflow: hidden;
}
.step {
background: var(–linen);
padding: 52px 40px;
position: relative;
}
.step-num {
font-family: ‘Lora’, serif;
font-style: italic; font-size: 4rem;
color: rgba(74,17,40,.08); line-height: 1;
margin-bottom: 20px; display: block;
}
.step-title {
font-family: ‘Lora’, serif;
font-weight: 300; font-size: 1.5rem;
color: var(–ink); margin-bottom: 12px;
}
.step-body {
font-size: .85rem; line-height: 1.85; color: var(–muted);
}

/* ── QUOTE ── */
.quote-section {
padding: 120px 60px;
display: flex; align-items: center; justify-content: center;
text-align: center;
}
.quote-inner { max-width: 760px; }
.quote-mark {
font-family: ‘Lora’, serif;
font-size: 6rem; color: var(–blush);
line-height: .5; display: block; margin-bottom: 32px;
}
.quote-text {
font-family: ‘Lora’, serif;
font-style: italic; font-weight: 300;
font-size: clamp(1.8rem, 3vw, 2.6rem);
color: var(–ink); line-height: 1.45;
}
.quote-text em { color: #3AADA8; font-style: normal; }
.quote-line {
width: 48px; height: 1px;
background: var(–rose); margin: 32px auto 20px;
}
.quote-author {
font-size: .72rem; letter-spacing: .18em;
text-transform: uppercase; color: var(–muted);
}

/* ── BLOG / RECURSOS ── */
.blog-grid {
display: grid;
grid-template-columns: 1.4fr 1fr 1fr;
gap: 24px;
margin-top: 64px;
}
.blog-card {
border-radius: 16px; overflow: hidden;
border: 1px solid rgba(74,17,40,.07);
display: flex; flex-direction: column;
transition: border-color .25s, transform .25s;
text-decoration: none;
}
.blog-card:hover { border-color: var(–rose); transform: translateY(-4px); }
.blog-card-img {
height: 220px; background: var(–linen);
display: flex; align-items: center; justify-content: center;
position: relative; overflow: hidden;
}
.blog-card.featured .blog-card-img { height: 280px; background: var(–burg); }
.blog-card-img-text {
font-family: ‘Lora’, serif;
font-style: italic; font-weight: 300;
font-size: 3.2rem; color: rgba(74,17,40,.12);
letter-spacing: .3em;
}
.blog-card.featured .blog-card-img-text { color: rgba(245,240,235,.12); }
.blog-card-body { padding: 28px 28px 32px; flex: 1; display: flex; flex-direction: column; gap: 10px; background: var(–cream); }
.blog-card-tag {
font-size: .62rem; font-weight: 500;
letter-spacing: .2em; text-transform: uppercase; color: var(–rose);
}
.blog-card-title {
font-family: ‘Lora’, serif;
font-weight: 300; font-size: 1.3rem;
color: var(–ink); line-height: 1.3; flex: 1;
}
.blog-card-meta { font-size: .72rem; color: var(–muted); }
.blog-card-arrow {
font-size: .8rem; color: var(–rose);
align-self: flex-start; margin-top: 8px;
}

/* ── CONTACTO ── */
.contacto { background: var(–burg); padding: 120px 60px; }
.contacto-inner {
max-width: 1280px; margin: 0 auto;
display: grid; grid-template-columns: 1fr 1fr;
gap: 100px; align-items: start;
}
.contacto-left {}
.contacto-eyebrow {
font-size: .62rem; font-weight: 500;
letter-spacing: .36em; text-transform: uppercase;
color: var(–blush); opacity: .7;
margin-bottom: 20px;
display: flex; align-items: center; gap: 12px;
}
.contacto-eyebrow::before { content: ‘’; width: 18px; height: 1px; background: var(–blush); opacity: .5; }
.contacto-title {
font-family: ‘Lora’, serif;
font-weight: 300;
font-size: clamp(2.4rem, 4vw, 3.4rem);
color: var(–cream); line-height: 1.2; margin-bottom: 24px;
}
.contacto-title em { font-style: italic; color: #3AADA8; }
.contacto-body {
font-size: .92rem; line-height: 1.9;
color: rgba(245,240,235,.55); max-width: 380px;
margin-bottom: 40px;
}
.contacto-options { display: flex; flex-direction: column; gap: 14px; }
.contacto-btn {
display: flex; align-items: center; gap: 14px;
padding: 18px 28px;
border-radius: 12px;
background: rgba(245,240,235,.06);
border: 1px solid rgba(245,240,235,.1);
color: var(–cream); text-decoration: none;
font-size: .82rem; letter-spacing: .06em;
transition: background .25s, border-color .25s;
}
.contacto-btn:hover { background: rgba(245,240,235,.1); border-color: rgba(245,240,235,.2); }
.contacto-btn-icon { font-size: 1.1rem; }

/* form */
.contacto-form { display: flex; flex-direction: column; gap: 16px; }
.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.form-group { display: flex; flex-direction: column; gap: 8px; }
.form-label {
font-size: .62rem; font-weight: 500;
letter-spacing: .18em; text-transform: uppercase;
color: rgba(245,240,235,.4);
}
.form-input, .form-select, .form-textarea {
background: rgba(245,240,235,.06);
border: 1px solid rgba(245,240,235,.12);
border-radius: 10px;
padding: 14px 18px;
color: var(–cream);
font-family: ‘DM Sans’, sans-serif;
font-size: .88rem; font-weight: 300;
outline: none;
transition: border-color .2s, background .2s;
appearance: none;
}
.form-input::placeholder, .form-textarea::placeholder { color: rgba(245,240,235,.25); }
.form-input:focus, .form-select:focus, .form-textarea:focus {
border-color: rgba(232,180,192,.4);
background: rgba(245,240,235,.09);
}
.form-select option { background: var(–mid); color: var(–cream); }
.form-textarea { resize: none; height: 120px; }
.form-submit {
font-size: .72rem; font-weight: 500;
letter-spacing: .14em; text-transform: uppercase;
color: var(–burg); background: var(–cream);
padding: 16px 36px; border-radius: 100px;
border: none; cursor: none;
align-self: flex-start; margin-top: 8px;
transition: background .25s;
}
.form-submit:hover { background: var(–linen); }

/* ── FOOTER ── */
footer {
background: var(–mid);
padding: 64px 60px 40px;
}
.footer-inner {
max-width: 1280px; margin: 0 auto;
}
.footer-top {
display: flex; align-items: flex-start;
justify-content: space-between; gap: 60px;
padding-bottom: 48px;
border-bottom: 1px solid rgba(245,240,235,.07);
}
.footer-logo {
font-family: ‘Lora’, serif;
font-weight: 300; font-size: 2.4rem;
letter-spacing: .42em; color: var(–cream);
}
.footer-tagline {
font-family: ‘Lora’, serif;
font-style: italic; font-size: .95rem;
color: rgba(245,240,235,.35); margin-top: 8px;
}
.footer-nav { display: flex; gap: 60px; }
.footer-nav-group { display: flex; flex-direction: column; gap: 12px; }
.footer-nav-title {
font-size: .62rem; font-weight: 500;
letter-spacing: .24em; text-transform: uppercase;
color: var(–rose); margin-bottom: 4px;
}
.footer-nav-link {
font-size: .82rem; color: rgba(245,240,235,.4);
text-decoration: none; transition: color .2s;
}
.footer-nav-link:hover { color: var(–cream); }
.footer-bottom {
display: flex; align-items: center; justify-content: space-between;
padding-top: 28px;
}
.footer-copy {
font-size: .72rem; color: rgba(245,240,235,.22);
letter-spacing: .06em;
}
.footer-legal {
display: flex; gap: 28px;
}
.footer-legal a {
font-size: .68rem; color: rgba(245,240,235,.22);
text-decoration: none; letter-spacing: .06em;
transition: color .2s;
}
.footer-legal a:hover { color: rgba(245,240,235,.5); }

/* divider */
.divider { height: 1px; background: rgba(74,17,40,.08); }

/* body text always DM Sans */
.hero-body, .servicio-body, .perfil-body, .step-body,
.contacto-body, .blog-card-title, .hero-stat-label {
font-family: ‘DM Sans’, sans-serif;
font-weight: 300;
}
/* titles & display always Cormorant */
.section-title, .servicio-title, .perfil-title, .step-title,
.contacto-title, .hero-headline, .footer-logo, .nav-logo,
.hero-wordmark, .hero-stat-num, .quote-text,
.intro-strip-text {
font-family: ‘Cormorant Garamond’, serif;
}

/* ── ANIMATIONS ── */
@keyframes fadeUp {
from { opacity: 0; transform: translateY(22px); }
to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
from { opacity: 0; }
to   { opacity: 1; }
}

/* ── RESPONSIVE ── */
@media (max-width: 900px) {
nav { padding: 18px 24px; }
.nav-links { display: none; }
.hero { grid-template-columns: 1fr; padding: 120px 24px 60px; gap: 48px; }
.hero::after { display: none; }
.section { padding: 80px 24px; }
.servicios-grid { grid-template-columns: 1fr; }
.servicio-card.featured { grid-column: 1; grid-template-columns: 1fr; }
.perfiles-grid { grid-template-columns: 1fr 1fr; }
.proceso-steps { grid-template-columns: 1fr; }
.blog-grid { grid-template-columns: 1fr; }
.contacto { padding: 80px 24px; }
.contacto-inner { grid-template-columns: 1fr; gap: 48px; }
.form-row { grid-template-columns: 1fr; }
.footer-top { flex-direction: column; }
.footer-nav { flex-direction: column; gap: 32px; }
.intro-strip { padding: 40px 24px; flex-direction: column; }
}

</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="ring"></div>

<!-- NAV -->

<nav>
  <a href="#" class="nav-logo">abra</a>
  <div class="nav-links">
    <a href="#servicios" class="nav-link">Servicios</a>
    <a href="#para-quien" class="nav-link">Para quién</a>
    <a href="#como-funciona" class="nav-link">Cómo funciona</a>
    <a href="#blog" class="nav-link">Recursos</a>
    <a href="#contacto" class="nav-cta">Hablemos</a>
  </div>
</nav>

<!-- HERO -->

<section class="hero">
  <div class="hero-left">
    <p class="hero-eyebrow">Studio legal · México</p>
    <span class="hero-wordmark">abra</span>
    <h1 class="hero-headline">
      Tu nombre, tu música,<br>tu libro, tu diseño —<br><span style="color:#3AADA8;font-style:normal;">Protégelos.</span>
    </h1>
    <p class="hero-body">
      Creaste algo tuyo. Ahora asegúrate de que Nadie más pueda usarlo, copiarlo o registrarlo antes que tú. En abrah protegemos marcas, obras creativas y productos — para que lo que construiste siga siendo tuyo.
    </p>
    <div class="hero-actions">
      <a href="#contacto" class="btn-primary">Agenda una consulta</a>
      <a href="#servicios" class="btn-ghost">Ver servicios</a>
    </div>
  </div>
  <div class="hero-right">
    <div class="hero-stat" style="background:var(--linen);">
      <p style="font-family:'Lora',serif;font-style:italic;font-size:1.05rem;color:var(--burg);line-height:1.8;">
        "Protegemos lo que construiste — para que sigas construyendo."
      </p>
    </div>
    <div class="hero-stat">
      <span class="hero-stat-num" style="font-size:1.5rem;letter-spacing:.04em;">Marcas</span>
      <span class="hero-stat-label">Registro, renovaciones, cesiones, licencias y más ante el IMPI</span>
    </div>
    <div class="hero-stat">
      <span class="hero-stat-num" style="font-size:1.5rem;letter-spacing:.04em;">Derechos de autor</span>
      <span class="hero-stat-label">Obras, reservas, contratos — para artistas y creativos</span>
    </div>
    <div class="hero-stat">
      <span class="hero-stat-num" style="font-size:1.5rem;letter-spacing:.04em;">Regulatorio</span>
      <span class="hero-stat-label">Registros sanitarios, etiquetado y permisos COFEPRIS</span>
    </div>
  </div>
</section>

<!-- INTRO STRIP -->

<div class="intro-strip">
  <p class="intro-strip-text">
    El derecho creativo no es solo para<br>
    <em>Corporativos.</em> Es para ti,<br>
    que construiste algo desde cero.
  </p>
  <span class="intro-strip-logo">abra</span>
</div>

<!-- SERVICIOS -->

<section class="section" id="servicios">
  <p class="section-eyebrow">Servicios</p>
  <h2 class="section-title">Todo lo que necesitas para <em>Proteger tu negocio</em></h2>

  <div class="servicios-grid">

```
<!-- FEATURED: MARCAS -->
<div class="servicio-card featured">
  <div>
    <span class="servicio-num">01</span>
    <h3 class="servicio-title">Marcas ante el IMPI</h3>
    <p class="servicio-body">
      Tu nombre, logo o slogan registrado oficialmente en México. El primer paso para que Nadie más pueda usarlo — y tú puedas defenderlo si alguien lo intenta.
    </p>
  </div>
  <div class="servicio-items">
    <span class="servicio-item">Examen de viabilidad de marca</span>
    <span class="servicio-item">Registro de marca</span>
    <span class="servicio-item">Renovaciones y declaraciones de uso</span>
    <span class="servicio-item">Cesión y licencias de derechos</span>
    <span class="servicio-item">Oposiciones y nulidades</span>
    <span class="servicio-item">Clasificación de Niza</span>
    <span class="servicio-item">Marcas colectivas y de certificación</span>
  </div>
</div>

<!-- DERECHOS DE AUTOR -->
<div class="servicio-card">
  <span class="servicio-num">02</span>
  <h3 class="servicio-title">Derechos de autor</h3>
  <p class="servicio-body">
    Tu música, tu diseño, tu fotografía, tu código — todo es tuyo desde que lo creas. Pero registrarlo es lo que te da poder para defenderlo.
  </p>
  <div class="servicio-items">
    <span class="servicio-item">Registro de obras ante INDAUTOR</span>
    <span class="servicio-item">Reservas de derechos</span>
    <span class="servicio-item">Contratos de licencia y cesión</span>
    <span class="servicio-item">Obras musicales, literarias, visuales</span>
    <span class="servicio-item">Software y contenido digital</span>
  </div>
  <span class="servicio-arrow">→</span>
</div>

<!-- REGULATORIO -->
<div class="servicio-card">
  <span class="servicio-num">03</span>
  <h3 class="servicio-title">Registros y regulatorio</h3>
  <p class="servicio-body">
    Tienes un producto y quieres venderlo en tiendas, exportarlo o entrar a marketplaces. Te ayudamos a navegar los permisos sin perder tiempo ni dinero.
  </p>
  <div class="servicio-items">
    <span class="servicio-item">Registros sanitarios COFEPRIS</span>
    <span class="servicio-item">Alimentos, cosméticos, suplementos</span>
    <span class="servicio-item">Etiquetado y NOM's</span>
    <span class="servicio-item">Permisos de importación / exportación</span>
  </div>
  <span class="servicio-arrow">→</span>
</div>
```

  </div>
</section>

<div class="divider"></div>

<!-- PARA QUIÉN -->

<section class="para-quien" id="para-quien">
  <div class="section">
    <p class="section-eyebrow">Para quién</p>
    <h2 class="section-title">Si construiste algo, <em>Esto es para ti</em></h2>

```
<div class="perfiles-grid">
  <div class="perfil-card">
    <div class="perfil-icon">🚀</div>
    <h3 class="perfil-title">El fundador que está empezando</h3>
    <p class="perfil-body">Tienes el nombre, el logo y las ganas. Ahora necesitas que sea oficialmente tuyo antes de que alguien más lo registre.</p>
    <span class="perfil-tag">Registro · Búsqueda</span>
  </div>
  <div class="perfil-card">
    <div class="perfil-icon">📈</div>
    <h3 class="perfil-title">El negocio que ya creció</h3>
    <p class="perfil-body">Llevas años construyendo sin formalizar tu IP. Ahora quieres escalar, buscar inversión o entrar a retail — y necesitas tener todo en orden.</p>
    <span class="perfil-tag">Estructura · Contratos</span>
  </div>
  <div class="perfil-card">
    <div class="perfil-icon">🎨</div>
    <h3 class="perfil-title">El creativo y el artista</h3>
    <p class="perfil-body">Tu música, diseño, fotografía o contenido es tu trabajo. Mereces saber que es tuyo legalmente y tener las herramientas para defenderlo.</p>
    <span class="perfil-tag">Derechos de autor · Obras</span>
  </div>
  <div class="perfil-card">
    <div class="perfil-icon">📦</div>
    <h3 class="perfil-title">El emprendedor de producto</h3>
    <p class="perfil-body">Tienes alimentos, cosméticos o suplementos y quieres vender en canales formales. Te guiamos por los registros sanitarios y el etiquetado sin dramas.</p>
    <span class="perfil-tag">COFEPRIS · NOM · Etiquetado</span>
  </div>
</div>
```

  </div>
</section>

<div class="divider"></div>

<!-- QUOTE -->

<div class="quote-section">
  <div class="quote-inner">
    <span class="quote-mark">"</span>
    <p class="quote-text">
      Registrar tu marca no es un gasto.<br>Es la inversión más barata que puedes hacer<br>Por <em>todo lo que ya construiste.</em>
    </p>
    <div class="quote-line"></div>
    <p class="quote-author">abra · studio legal</p>
  </div>
</div>

<div class="divider"></div>

<!-- CÓMO FUNCIONA -->

<section class="section" id="como-funciona">
  <p class="section-eyebrow">Cómo funciona</p>
  <h2 class="section-title">Sin tecnicismos. <em>Sin complicaciones.</em> Con resultados.</h2>

  <div class="proceso-steps">
    <div class="step">
      <span class="step-num">01</span>
      <h3 class="step-title">Hablamos</h3>
      <p class="step-body">Agendas una consulta. Nos cuentas qué tienes, qué quieres proteger y en qué etapa está tu negocio. Sin formularios interminables, sin reuniones de una hora que no llevan a nada.</p>
    </div>
    <div class="step">
      <span class="step-num">02</span>
      <h3 class="step-title">Te explicamos qué necesitas</h3>
      <p class="step-body">Te decimos exactamente qué trámites aplican para tu caso, cuánto cuestan, cuánto tardan y qué pasa en cada paso. Sin letra chica. Sin sorpresas.</p>
    </div>
    <div class="step">
      <span class="step-num">03</span>
      <h3 class="step-title">Nosotros lo hacemos</h3>
      <p class="step-body">Tú sigues construyendo tu negocio. Nosotros nos encargamos del papeleo, los plazos y los seguimientos. Te avisamos cuando hay algo que necesites saber.</p>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- BLOG / RECURSOS -->

<section class="section" id="blog">
  <p class="section-eyebrow">Recursos</p>
  <h2 class="section-title">Aprende lo que nadie<br><em>Te había explicado</em></h2>

  <div class="blog-grid">
    <a href="#" class="blog-card featured">
      <div class="blog-card-img">
        <span class="blog-card-img-text">abra</span>
      </div>
      <div class="blog-card-body">
        <span class="blog-card-tag">marcas</span>
        <h3 class="blog-card-title">¿Qué pasa si alguien registra tu nombre antes que tú?</h3>
        <p class="blog-card-meta">5 min de lectura</p>
        <span class="blog-card-arrow">→ Leer</span>
      </div>
    </a>
    <a href="#" class="blog-card">
      <div class="blog-card-img">
        <span class="blog-card-img-text" style="font-size:2rem;">©</span>
      </div>
      <div class="blog-card-body">
        <span class="blog-card-tag">Derechos de autor</span>
        <h3 class="blog-card-title">Tu música es tuya desde que la creas. Esto es lo que debes saber.</h3>
        <p class="blog-card-meta">4 min de lectura</p>
        <span class="blog-card-arrow">→ Leer</span>
      </div>
    </a>
    <a href="#" class="blog-card">
      <div class="blog-card-img">
        <span class="blog-card-img-text" style="font-size:2rem;">⬡</span>
      </div>
      <div class="blog-card-body">
        <span class="blog-card-tag">regulatorio</span>
        <h3 class="blog-card-title">Quiero vender mi producto en tiendas. ¿Qué necesito primero?</h3>
        <p class="blog-card-meta">6 min de lectura</p>
        <span class="blog-card-arrow">→ Leer</span>
      </div>
    </a>
  </div>
</section>

<!-- CONTACTO -->

<section class="contacto" id="contacto">
  <div class="contacto-inner">
    <div class="contacto-left">
      <p class="contacto-eyebrow">Hablemos</p>
      <h2 class="contacto-title">
        cuéntanos<br>qué estás<br><em>construyendo.</em>
      </h2>
      <p class="contacto-body">
        Sin compromiso. Sin tecnicismos. Solo una conversación honesta sobre cómo podemos proteger lo que ya construiste.
      </p>
      <div class="contacto-options">
        <a href="https://wa.me/521XXXXXXXXXX" class="contacto-btn">
          <span class="contacto-btn-icon">💬</span>
          Escríbenos por WhatsApp
        </a>
        <a href="/cdn-cgi/l/email-protection#9ff7f0f3fedffefdedfef7b1ecebeafbf6f0" class="contacto-btn">
          <span class="contacto-btn-icon">✉️</span>
          <span class="__cf_email__" data-cfemail="492126252809282b3b2821673a3d3c2d2026">[email&#160;protected]</span>
        </a>
      </div>
    </div>

```
<form class="contacto-form">
  <div class="form-row">
    <div class="form-group">
      <label class="form-label">nombre</label>
      <input type="text" class="form-input" placeholder="Tu nombre">
    </div>
    <div class="form-group">
      <label class="form-label">email</label>
      <input type="email" class="form-input" placeholder="tu@email.com">
    </div>
  </div>
  <div class="form-group">
    <label class="form-label">¿qué necesitas?</label>
    <select class="form-select">
      <option value="" disabled selected>Selecciona un servicio</option>
      <option>Registro de marca</option>
      <option>Renovación o declaración de uso</option>
      <option>Cesión o licencia de marca</option>
      <option>Derechos de autor</option>
      <option>Registro sanitario COFEPRIS</option>
      <option>Etiquetado / NOM</option>
      <option>No sé, necesito orientación</option>
    </select>
  </div>
  <div class="form-group">
    <label class="form-label">cuéntanos un poco</label>
    <textarea class="form-textarea" placeholder="¿En qué etapa está tu negocio? ¿Qué quieres proteger?"></textarea>
  </div>
  <button type="submit" class="form-submit">Enviar mensaje →</button>
</form>
```

  </div>
</section>

<!-- FOOTER -->

<footer>
  <div class="footer-inner">
    <div class="footer-top">
      <div>
        <div class="footer-logo">abra</div>
        <p class="footer-tagline">Lo que construiste, es tuyo.</p>
      </div>
      <div class="footer-nav">
        <div class="footer-nav-group">
          <span class="footer-nav-title">Servicios</span>
          <a href="#" class="footer-nav-link">Examen de viabilidad de marca</a>
          <a href="#" class="footer-nav-link">Derechos de autor</a>
          <a href="#" class="footer-nav-link">Registros sanitarios</a>
          <a href="#" class="footer-nav-link">Contratos</a>
        </div>
        <div class="footer-nav-group">
          <span class="footer-nav-title">Recursos</span>
          <a href="#" class="footer-nav-link">Blog</a>
          <a href="#" class="footer-nav-link">Guías gratuitas</a>
          <a href="#" class="footer-nav-link">Preguntas frecuentes</a>
        </div>
        <div class="footer-nav-group">
          <span class="footer-nav-title">Contacto</span>
          <a href="#" class="footer-nav-link"><span class="__cf_email__" data-cfemail="b1d9deddd0f1d0d3c3d0d99fc2c5c4d5d8de">[email&#160;protected]</span></a>
          <a href="#" class="footer-nav-link">WhatsApp</a>
          <a href="#" class="footer-nav-link">Agendar consulta</a>
          <a href="#" class="footer-nav-link">Instagram</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p class="footer-copy">© 2025 abrah Studio legal · México</p>
      <div class="footer-legal">
        <a href="#">Aviso de privacidad</a>
        <a href="#">Términos de servicio</a>
      </div>
    </div>
  </div>
</footer>

<script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>

const cur = document.getElementById(‘cursor’);
const rng = document.getElementById(‘ring’);
let mx=0,my=0,rx=0,ry=0;
document.addEventListener(‘mousemove’,e=>{
mx=e.clientX; my=e.clientY;
cur.style.left=mx+‘px’; cur.style.top=my+‘px’;
});
(function anim(){
rx+=(mx-rx)*.1; ry+=(my-ry)*.1;
rng.style.left=rx+‘px’; rng.style.top=ry+‘px’;
requestAnimationFrame(anim);
})();
document.querySelectorAll(‘a,button,.servicio-card,.perfil-card,.blog-card’).forEach(el=>{
el.addEventListener(‘mouseenter’,()=>{cur.style.width=‘14px’;cur.style.height=‘14px’;});
el.addEventListener(‘mouseleave’,()=>{cur.style.width=‘8px’;cur.style.height=‘8px’;});
});

// simple scroll reveal
const observer = new IntersectionObserver(entries=>{
entries.forEach(e=>{
if(e.isIntersecting){
e.target.style.opacity=‘1’;
e.target.style